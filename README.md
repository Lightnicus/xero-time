# Xero Time

Single-business project time tracking built with Next.js, Payload CMS, MongoDB, and Xero.

The deployable application is in [`app/`](app/). Keep Vercel's Root Directory set to `app` when the hosted environments are created.

## Development

See [`app/README.md`](app/README.md) for the local setup and verification commands.

The detailed delivery plan is in [`plans/implementation-plan.md`](plans/implementation-plan.md).
