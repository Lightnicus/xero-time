# ADR 0001: Payload application framework

Status: accepted

Use Payload inside the Next.js deployment for schema, authentication, Local API, Admin, version history, and jobs. Domain commands still own authorization and state transitions; generated Admin configuration is presentation, not a security boundary.
