# Redacted Xero contract fixtures

These fixtures preserve the response shapes used by the parser and fake-provider tests. All tenant, user, contact, invoice, token, nonce, and subject values are synthetic. Replace or add fixtures only after redacting credentials, personal data, free-text descriptions, and organisation identifiers.
